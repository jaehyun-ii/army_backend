# 프론트엔드 웹 애플리케이션 시나리오

이 문서는 웹 UI에서 사용자가 어떤 페이지에서 어떤 조건을 선택하고, 어떤 API를 호출하는지 상세히 설명합니다.

## ⚠️ 중요: 파라미터 관리 정책

**학습 및 공격 파라미터는 백엔드에서 관리됩니다.**

프론트엔드는 다음 항목만 입력:
- ✅ 데이터셋 선택
- ✅ 모델 선택
- ✅ 타겟 클래스 선택
- ✅ 이름/설명 입력
- ❌ 학습 파라미터 (epsilon, iterations 등) - **백엔드 기본값 사용**

자세한 내용: [PARAMETER_MANAGEMENT.md](./PARAMETER_MANAGEMENT.md)

---

## 목차

1. [전체 페이지 구조](#전체-페이지-구조)
2. [시나리오 1: 2D 적대적 패치 생성](#시나리오-1-2d-적대적-패치-생성)
3. [시나리오 2: 노이즈 공격 데이터셋 생성](#시나리오-2-노이즈-공격-데이터셋-생성)
4. [시나리오 3: 모델 평가 및 비교](#시나리오-3-모델-평가-및-비교)
5. [시나리오 4: 실시간 카메라 탐지](#시나리오-4-실시간-카메라-탐지)
6. [시나리오 5: 실험 관리](#시나리오-5-실험-관리)

---

## 전체 페이지 구조

```
📱 Adversarial Vision Platform

├── 🏠 Dashboard (홈)
├── 📊 Datasets
│   ├── 2D Datasets
│   ├── 3D Datasets
│   └── Upload Dataset
├── 🤖 Models
│   ├── Model Repository
│   └── Upload Model
├── ⚔️ Attacks
│   ├── Adversarial Patch
│   ├── Noise Attacks (FGSM, PGD, Gaussian)
│   └── Plugin Attacks
├── 📈 Evaluation
│   ├── Run Evaluation
│   ├── Evaluation History
│   └── Compare Results
├── 📹 Real-time
│   ├── Cameras
│   └── Live Sessions
├── 🧪 Experiments
│   ├── Experiment List
│   └── Create Experiment
└── 📊 Benchmarks
```

---

## 시나리오 1: 2D 적대적 패치 생성

### 페이지: `/attacks/adversarial-patch/create`

#### 1.1 페이지 진입 시 (Initial Load)

**UI 구성:**
```
┌─────────────────────────────────────────────────────┐
│ 📝 Create Adversarial Patch                         │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Step 1: Select Source Dataset                       │
│ ┌─────────────────────────────────────────────┐    │
│ │ Select Dataset: [Dropdown ▼]                │    │
│ │                 - Loading datasets...        │    │
│ └─────────────────────────────────────────────┘    │
│                                                      │
│ Step 2: Select Model                                │
│ ┌─────────────────────────────────────────────┐    │
│ │ Select Model: [Dropdown ▼]                  │    │
│ │               - Loading models...            │    │
│ └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**API 호출 1: 데이터셋 목록 로드**
```javascript
// Component: PatchCreatePage.jsx
useEffect(() => {
  fetchDatasets();
}, []);

async function fetchDatasets() {
  const response = await fetch('/api/v1/datasets-2d?skip=0&limit=100', {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const datasets = await response.json();
  setDatasetOptions(datasets);
}
```

**API 호출 2: 모델 목록 로드**
```javascript
async function fetchModels() {
  const response = await fetch('/api/v1/models?skip=0&limit=100', {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const models = await response.json();
  setModelOptions(models);
}
```

**API 호출 3: 데이터셋의 상위 클래스 로드 (새 기능)**
```javascript
// 사용자가 데이터셋을 선택하면 자동으로 호출
async function fetchTopClasses(datasetId) {
  const response = await fetch(
    `/api/v1/datasets-2d/${datasetId}/top-classes?limit=5`,
    {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );
  const data = await response.json();

  if (data.source === 'metadata') {
    // 메타데이터 기반 - 즉시 표시 (10ms)
    setTopClasses(data.top_classes);
    setClassOptions(data.top_classes.map(c => c.class_name));
  } else {
    // 메타데이터 없음 - 수동 입력 또는 모델 추론 필요
    setTopClasses([]);
    setClassOptions([]); // 사용자 직접 입력
  }
}

// 응답 예시
{
  "dataset_id": "abc123-dataset-id",
  "dataset_name": "COCO Person Training Set",
  "total_images": 150,
  "top_classes": [
    {
      "class_name": "person",
      "count": 2134,
      "percentage": 65.75,
      "avg_confidence": 0.872,
      "image_count": 145
    },
    // ... 최대 5개
  ],
  "source": "metadata",
  "cached": true
}
```

**✨ 성능 개선:**
- 메타데이터 기반 조회: **750배 빠름** (7.5초 → 10ms)
- 사전 계산된 통계로 즉시 응답
- 타겟 클래스 선택 시 데이터 기반 추천 가능

---

#### 1.2 사용자 입력 단계

**UI 구성 (간소화 버전):**
```
┌─────────────────────────────────────────────────────────────┐
│ 📝 Create Adversarial Patch                                  │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ 🎯 Basic Configuration                                       │
│                                                               │
│ Patch Name *                                                 │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Person Invisibility Patch v1                          │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Source Dataset *                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] Select Dataset                                    │   │
│ │     ├─ COCO Person Training Set (150 images)         │   │
│ │     ├─ Custom Dataset A (234 images)                 │   │
│ │     └─ Street Scene Dataset (89 images)              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Target Model *                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] Select Model                                      │   │
│ │     ├─ YOLOv8n-COCO v1.0.0 (6.2 MB)                  │   │
│ │     ├─ YOLOv8s-COCO v1.0.0 (22.5 MB)                 │   │
│ │     └─ Custom Detection Model v2.1 (15.3 MB)         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Target Class *                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] Select Class (Recommended from dataset)          │   │
│ │     🌟 Top 5 classes in selected dataset:            │   │
│ │     ├─ 👤 person (2134 detections, 65.75%)           │   │
│ │     ├─ 🚗 car (567 detections, 17.47%)               │   │
│ │     ├─ 🚴 bicycle (234 detections, 7.21%)            │   │
│ │     ├─ 🐕 dog (156 detections, 4.81%)                │   │
│ │     └─ 🚦 traffic light (154 detections, 4.75%)      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Description (Optional)                                       │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Universal patch to evade person detection            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ℹ️ Training parameters are managed by the system            │
│    Default: patch_size=100px, epsilon=0.6, iterations=100   │
│                                                               │
│ ┌──────────────┐  ┌──────────────┐                         │
│ │   Cancel     │  │ Generate ⚡  │                         │
│ └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

**간소화된 Form State:**
```javascript
const [formData, setFormData] = useState({
  patchName: '',
  datasetId: null,
  modelVersionId: null,
  targetClass: '',
  description: ''
  // ✅ 학습 파라미터는 백엔드에서 관리
});
```

---

#### 1.3 "Generate" 버튼 클릭 시

**사용자 액션:** "Generate ⚡" 버튼 클릭

**UI 변화:**
```
┌─────────────────────────────────────────────────────┐
│ 🔄 Generating Patch...                              │
├─────────────────────────────────────────────────────┤
│                                                      │
│ ⏳ Patch Generation in Progress                     │
│                                                      │
│ Status: Training adversarial patch                  │
│ Progress: ████████░░░░░░░░░░  45%                  │
│                                                      │
│ Estimated time remaining: 2m 15s                    │
│                                                      │
│ ┌────────────────────────────────────────────┐     │
│ │ Iteration 45/100                            │     │
│ │ Current Loss: 0.234                         │     │
│ │ Images Processed: 68/150                    │     │
│ └────────────────────────────────────────────┘     │
│                                                      │
│ [  Cancel Generation  ]                             │
└─────────────────────────────────────────────────────┘
```

**간소화된 API 호출:**
```javascript
async function handleGeneratePatch(e) {
  e.preventDefault();

  // Validation
  if (!formData.datasetId || !formData.modelVersionId || !formData.targetClass) {
    toast.error('Please fill all required fields');
    return;
  }

  setIsGenerating(true);
  setProgress(0);

  try {
    // API Request (필수 항목만)
    const response = await fetch('/api/v1/adversarial-patch/patches/generate', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({
        patch_name: formData.patchName,
        model_version_id: formData.modelVersionId,
        dataset_id: formData.datasetId,
        target_class: formData.targetClass,
        description: formData.description
        // ✅ 학습 파라미터는 백엔드 기본값 사용
      })
    });

    if (!response.ok) {
      throw new Error('Failed to generate patch');
    }

    const result = await response.json();

    // 사용된 파라미터 확인 가능
    console.log('Parameters used:', result.parameters_used);

    // Success handling
    setGeneratedPatch(result);
    toast.success('Patch generated successfully!');

    // Navigate to result page
    navigate(`/attacks/adversarial-patch/result/${result.patch.id}`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  } finally {
    setIsGenerating(false);
  }
}
```

**진행 상태 모니터링 (WebSocket - Optional):**
```javascript
// WebSocket for real-time progress
useEffect(() => {
  if (taskId) {
    const ws = new WebSocket(`ws://localhost:8000/api/v1/tasks/${taskId}/progress`);

    ws.onmessage = (event) => {
      const data = JSON.parse(event.data);
      setProgress(data.progress);
      setCurrentIteration(data.iteration);
      setCurrentLoss(data.loss);
    };

    return () => ws.close();
  }
}, [taskId]);
```

---

#### 1.4 결과 페이지

**페이지: `/attacks/adversarial-patch/result/:patchId`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ ✅ Patch Generated Successfully                              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ 📊 Patch Information                                         │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Name: Person Invisibility Patch v1                    │   │
│ │ Target Class: person                                  │   │
│ │ Created: 2024-10-04 10:15:23                         │   │
│ │ File Size: 256 KB                                     │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ 🖼️ Patch Preview                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │                                                        │   │
│ │        [  Patch Image Preview  ]                      │   │
│ │                                                        │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ 📈 Generation Statistics                                     │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Total Iterations: 100                                 │   │
│ │ Final Loss: 0.123                                     │   │
│ │ Training Time: 45.2 seconds                           │   │
│ │ Images Processed: 150                                 │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ 🎬 Next Actions                                              │
│ ┌──────────────────┐ ┌──────────────────┐                  │
│ │ Download Patch   │ │ Apply to Dataset │                  │
│ └──────────────────┘ └──────────────────┘                  │
│ ┌──────────────────┐ ┌──────────────────┐                  │
│ │ Run Evaluation   │ │ Create New Patch │                  │
│ └──────────────────┘ └──────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

**페이지 로드 시 API 호출:**
```javascript
useEffect(() => {
  fetchPatchDetails();
}, [patchId]);

async function fetchPatchDetails() {
  const response = await fetch(`/api/v1/adversarial-patch/patches/${patchId}`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });

  const patch = await response.json();
  setPatchData(patch);
}
```

**"Download Patch" 버튼 클릭:**
```javascript
async function handleDownloadPatch() {
  const response = await fetch(
    `/api/v1/adversarial-patch/patches/${patchId}/download`,
    {
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${token}`
      }
    }
  );

  const blob = await response.blob();
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `adversarial_patch_${patchId}.png`;
  a.click();
}
```

---

## 시나리오 2: 노이즈 공격 데이터셋 생성

### 페이지: `/attacks/noise-attack/create`

#### 2.1 공격 타입 선택

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 🎭 Create Noise Attack Dataset                              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Step 1: Select Attack Type                                  │
│ ┌──────────────────────────────────────────────────────┐   │
│ │  ○ FGSM (Fast Gradient Sign Method)                  │   │
│ │     Fast one-step attack using gradient sign         │   │
│ │                                                        │   │
│ │  ● PGD (Projected Gradient Descent)                   │   │
│ │     Iterative attack with multiple steps              │   │
│ │                                                        │   │
│ │  ○ Gaussian Noise                                     │   │
│ │     Random Gaussian noise injection                   │   │
│ │                                                        │   │
│ │  ○ Uniform Noise                                      │   │
│ │     Random uniform noise injection                    │   │
│ │                                                        │   │
│ │  ○ Salt & Pepper                                      │   │
│ │     Random black/white pixel noise                    │   │
│ └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**State 관리:**
```javascript
const [attackType, setAttackType] = useState('pgd'); // 'fgsm', 'pgd', 'gaussian', etc.
const [parameters, setParameters] = useState({});

// Attack type에 따라 다른 파라미터 표시
const getParametersForAttackType = (type) => {
  switch(type) {
    case 'fgsm':
      return { epsilon: 8.0, targeted: false, target_class: '' };
    case 'pgd':
      return { epsilon: 8.0, alpha: 2.0, iterations: 10, targeted: false };
    case 'gaussian':
      return { mean: 0.0, std: 0.1 };
    default:
      return {};
  }
};
```

---

#### 2.2 PGD 공격 파라미터 설정

**UI 구성 (PGD 선택 시):**
```
┌─────────────────────────────────────────────────────────────┐
│ 🎭 Create Noise Attack Dataset - PGD                        │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Attack Name *                                                │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ PGD Attack on COCO Person                             │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Source Dataset *                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] COCO Person Training Set (150 images)            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Target Model *                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] YOLOv8n-COCO v1.0.0                              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ PGD Parameters                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Epsilon (0-255)      [8.0    ] 🛈                     │   │
│ │ ├─ 0────────8────────────────────255                 │   │
│ │                                                        │   │
│ │ Alpha (Step Size)    [2.0    ] 🛈                     │   │
│ │ ├─ 0────2──────────────────────10                    │   │
│ │                                                        │   │
│ │ Iterations           [10     ] 🛈                     │   │
│ │ ├─ 1───10─────────────────────100                    │   │
│ │                                                        │   │
│ │ ☐ Targeted Attack                                     │   │
│ │   Target Class: [Disabled]                            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Description (Optional)                                       │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ PGD attack with epsilon=8, 10 iterations              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌──────────────┐  ┌──────────────┐                         │
│ │   Cancel     │  │ Generate 🚀  │                         │
│ └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

---

#### 2.3 "Generate" 버튼 클릭

**API 호출:**
```javascript
async function handleGenerateNoiseAttack(e) {
  e.preventDefault();

  setIsGenerating(true);

  try {
    const endpoint = `/api/v1/noise-attack/${attackType}/generate`;

    const requestBody = {
      attack_dataset_name: formData.attackName,
      model_version_id: formData.modelVersionId,
      base_dataset_id: formData.datasetId,
      description: formData.description,
      ...getAttackSpecificParams()
    };

    const response = await fetch(endpoint, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(requestBody)
    });

    const result = await response.json();

    toast.success(`${attackType.toUpperCase()} attack dataset created!`);
    navigate(`/attacks/noise-attack/result/${result.attack_dataset.id}`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  } finally {
    setIsGenerating(false);
  }
}

// 공격 타입별 파라미터 생성
function getAttackSpecificParams() {
  switch(attackType) {
    case 'fgsm':
      return {
        epsilon: formData.epsilon,
        targeted: formData.targeted,
        target_class: formData.targeted ? formData.targetClass : null
      };
    case 'pgd':
      return {
        epsilon: formData.epsilon,
        alpha: formData.alpha,
        iterations: formData.iterations,
        targeted: formData.targeted,
        target_class: formData.targeted ? formData.targetClass : null
      };
    case 'gaussian':
      return {
        mean: formData.mean,
        std: formData.std
      };
    default:
      return {};
  }
}
```

---

## 시나리오 3: 모델 평가 및 비교

### 페이지: `/evaluation/create`

#### 3.1 평가 타입 선택

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 Create Evaluation Run                                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Evaluation Type *                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │  ● Pre-Attack (Baseline)                              │   │
│ │     Evaluate model on clean dataset                   │   │
│ │                                                        │   │
│ │  ○ Post-Attack                                        │   │
│ │     Evaluate model on adversarial dataset             │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Evaluation Name *                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Baseline Evaluation - YOLOv8n                         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Target Model *                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] YOLOv8n-COCO v1.0.0                              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Dataset *                                                    │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] COCO Person Training Set (150 images)            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Evaluation Parameters                                        │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Confidence Threshold  [0.25  ] 🛈                     │   │
│ │ IOU Threshold         [0.5   ] 🛈                     │   │
│ │                                                        │   │
│ │ Metrics to Calculate:                                 │   │
│ │ ☑ mAP (Mean Average Precision)                        │   │
│ │ ☑ Precision                                           │   │
│ │ ☑ Recall                                              │   │
│ │ ☑ F1 Score                                            │   │
│ │ ☐ Inference Time                                      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌──────────────┐  ┌──────────────┐                         │
│ │   Cancel     │  │ Run Evaluation │                       │
│ └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

---

#### 3.2 "Run Evaluation" 버튼 클릭

**API 호출:**
```javascript
async function handleRunEvaluation(e) {
  e.preventDefault();

  setIsRunning(true);

  try {
    const requestBody = {
      name: formData.evaluationName,
      phase: formData.evaluationType, // 'pre_attack' or 'post_attack'
      model_version_id: formData.modelVersionId,
      config: {
        confidence_threshold: formData.confidenceThreshold,
        iou_threshold: formData.iouThreshold
      }
    };

    // Pre-attack: base_dataset_id 사용
    if (formData.evaluationType === 'pre_attack') {
      requestBody.base_dataset_id = formData.datasetId;
    }
    // Post-attack: attack_dataset_id 사용
    else {
      requestBody.attack_dataset_id = formData.datasetId;
    }

    const response = await fetch('/api/v1/evaluation/runs', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(requestBody)
    });

    const result = await response.json();

    toast.success('Evaluation started!');

    // 평가 진행 상황 모니터링 페이지로 이동
    navigate(`/evaluation/runs/${result.id}`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  } finally {
    setIsRunning(false);
  }
}
```

---

#### 3.3 평가 진행 모니터링

**페이지: `/evaluation/runs/:runId`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 🔄 Evaluation in Progress                                    │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Run: Baseline Evaluation - YOLOv8n                          │
│ Status: ⚡ Running                                           │
│                                                               │
│ Progress                                                     │
│ ████████████████████░░░░  67% (100/150 images)              │
│                                                               │
│ Current Metrics (Preliminary)                                │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ mAP:       0.723                                      │   │
│ │ Precision: 0.81                                       │   │
│ │ Recall:    0.76                                       │   │
│ │ F1 Score:  0.78                                       │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Estimated Time Remaining: 1m 23s                            │
│                                                               │
│ [  Stop Evaluation  ]                                        │
└─────────────────────────────────────────────────────────────┘
```

**주기적 API 폴링:**
```javascript
useEffect(() => {
  const pollInterval = setInterval(() => {
    fetchEvaluationStatus();
  }, 2000); // 2초마다 상태 확인

  return () => clearInterval(pollInterval);
}, [runId]);

async function fetchEvaluationStatus() {
  const response = await fetch(`/api/v1/evaluation/runs/${runId}`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });

  const run = await response.json();
  setEvaluationData(run);

  // 완료 시 폴링 중단
  if (run.status === 'completed' || run.status === 'failed') {
    setIsCompleted(true);
  }
}
```

---

#### 3.4 평가 결과 비교

**페이지: `/evaluation/compare`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 Compare Evaluation Results                               │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Pre-Attack Run                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] Baseline Evaluation - YOLOv8n                    │   │
│ │     (eval-pre-001) - Completed 2024-10-04            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Post-Attack Run                                              │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] Post-Attack - PGD Epsilon 8                      │   │
│ │     (eval-post-001) - Completed 2024-10-04           │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌──────────────┐                                             │
│ │ Compare 🔍   │                                             │
│ └──────────────┘                                             │
└─────────────────────────────────────────────────────────────┘
```

**"Compare" 버튼 클릭:**
```javascript
async function handleCompare() {
  setIsComparing(true);

  try {
    const response = await fetch('/api/v1/evaluation/runs/compare', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({
        pre_attack_run_id: preAttackRunId,
        post_attack_run_id: postAttackRunId
      })
    });

    const comparison = await response.json();
    setComparisonData(comparison);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  } finally {
    setIsComparing(false);
  }
}
```

**비교 결과 표시:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 Comparison Results                                        │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Metric Comparison                                            │
│ ┌──────────────┬──────────┬───────────┬─────────┐          │
│ │ Metric       │ Pre      │ Post      │ Delta   │          │
│ ├──────────────┼──────────┼───────────┼─────────┤          │
│ │ mAP          │ 0.745    │ 0.267     │ -64.2%  │ 📉      │
│ │ Precision    │ 0.82     │ 0.412     │ -49.8%  │ 📉      │
│ │ Recall       │ 0.78     │ 0.334     │ -57.2%  │ 📉      │
│ │ F1 Score     │ 0.80     │ 0.368     │ -54.0%  │ 📉      │
│ └──────────────┴──────────┴───────────┴─────────┘          │
│                                                               │
│ 🎯 Attack Success Rate: 64.2%                               │
│                                                               │
│ Class-wise Performance                                       │
│ ┌──────────────┬──────────┬───────────┬─────────┐          │
│ │ Class        │ Pre mAP  │ Post mAP  │ Drop    │          │
│ ├──────────────┼──────────┼───────────┼─────────┤          │
│ │ person       │ 0.856    │ 0.312     │ -63.6%  │          │
│ │ car          │ 0.789    │ 0.298     │ -62.2%  │          │
│ │ dog          │ 0.623    │ 0.201     │ -67.7%  │          │
│ └──────────────┴──────────┴───────────┴─────────┘          │
│                                                               │
│ ┌────────────────┐  ┌────────────────┐                     │
│ │ Export Report  │  │ Create Benchmark│                     │
│ └────────────────┘  └────────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 시나리오 4: 실시간 카메라 탐지

### 페이지: `/realtime/sessions/create`

#### 4.1 카메라 선택

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📹 Create Live Session                                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Step 1: Select Camera                                        │
│ ┌──────────────────────────────────────────────────────┐   │
│ │  ● Logitech Webcam C920 (webcam #0)                  │   │
│ │     Status: 🟢 Active | 1920x1080 @ 30fps            │   │
│ │     Location: Lab A - Desk 1                          │   │
│ │                                                        │   │
│ │  ○ Security Camera - Main Entrance (RTSP)            │   │
│ │     Status: 🟢 Active | 1920x1080 @ 25fps            │   │
│ │     Location: Building A - Main Entrance              │   │
│ │                                                        │   │
│ │  ○ IP Camera - Parking Lot (RTSP)                    │   │
│ │     Status: 🔴 Offline                                │   │
│ │                                                        │   │
│ │  + Add New Camera                                     │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ [  Next: Configure Session  ]                                │
└─────────────────────────────────────────────────────────────┘
```

**페이지 로드 시 카메라 목록 조회:**
```javascript
useEffect(() => {
  fetchCameras();
}, []);

async function fetchCameras() {
  const response = await fetch('/api/v1/realtime/cameras?skip=0&limit=100', {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });

  const cameras = await response.json();
  setCameraList(cameras);
}
```

---

#### 4.2 세션 설정

**UI 구성 (카메라 선택 후):**
```
┌─────────────────────────────────────────────────────────────┐
│ 📹 Create Live Session - Configuration                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Selected Camera: Logitech Webcam C920                       │
│                                                               │
│ Session Name *                                               │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Live Detection Demo                                   │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Detection Model *                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ [▼] YOLOv8n-COCO v1.0.0                              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Session Duration                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ● Unlimited (manual stop)                             │   │
│ │ ○ Fixed duration: [60] seconds                        │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Recording Options                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ☑ Save frames to disk                                │   │
│ │ Frame Sample Rate: Every [5] frames                   │   │
│ │                                                        │   │
│ │ ☐ Record video                                        │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Detection Parameters                                         │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Confidence Threshold  [0.5   ] 🛈                     │   │
│ │ IOU Threshold         [0.45  ] 🛈                     │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌──────────────┐  ┌──────────────┐                         │
│ │   Cancel     │  │ Start Session │                        │
│ └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

---

#### 4.3 "Start Session" 버튼 클릭

**API 호출:**
```javascript
async function handleStartSession(e) {
  e.preventDefault();

  setIsStarting(true);

  try {
    const requestBody = {
      camera_id: selectedCameraId,
      model_version_id: formData.modelVersionId,
      run_name: formData.sessionName,
      capture_duration_seconds: formData.isUnlimited ? null : formData.duration,
      frame_sample_rate: formData.frameSampleRate,
      save_frames: formData.saveFrames,
      config: {
        confidence_threshold: formData.confidenceThreshold,
        iou_threshold: formData.iouThreshold
      }
    };

    const response = await fetch('/api/v1/realtime/runs', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify(requestBody)
    });

    const session = await response.json();

    toast.success('Live session started!');

    // 라이브 스트림 페이지로 이동
    navigate(`/realtime/sessions/${session.id}/live`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  } finally {
    setIsStarting(false);
  }
}
```

---

#### 4.4 라이브 스트림 화면

**페이지: `/realtime/sessions/:sessionId/live`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 🔴 LIVE: Live Detection Demo                                │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ ┌───────────────────────────────────────────────────────┐  │
│ │                                                         │  │
│ │                                                         │  │
│ │                  📹 Live Video Feed                    │  │
│ │              [Real-time camera stream                  │  │
│ │               with bounding boxes]                     │  │
│ │                                                         │  │
│ │                                                         │  │
│ └───────────────────────────────────────────────────────┘  │
│                                                               │
│ Session Status: 🔴 LIVE | Duration: 00:02:34               │
│                                                               │
│ Real-time Metrics                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ FPS:              29.8                                │   │
│ │ Frames Processed: 4,782                               │   │
│ │ Total Detections: 456                                 │   │
│ │ Avg Det/Frame:    1.52                                │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Current Frame Detections                                     │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ 🟢 person (89%) - [120, 150, 280, 420]               │   │
│ │ 🟢 chair  (78%) - [400, 200, 550, 480]               │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Class Distribution (Last 100 frames)                        │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ person: ██████████ 42                                │   │
│ │ chair:  ██████░░░░ 31                                │   │
│ │ laptop: ████░░░░░░ 23                                │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌────────────────┐  ┌────────────────┐                     │
│ │  Stop Session  │  │ Take Snapshot  │                     │
│ └────────────────┘  └────────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

**WebSocket 연결:**
```javascript
useEffect(() => {
  // WebSocket 연결
  const wsUrl = `ws://localhost:8000/api/v1/realtime/stream/${sessionId}`;
  const ws = new WebSocket(wsUrl);

  ws.onopen = () => {
    console.log('WebSocket connected');
    setIsConnected(true);
  };

  ws.onmessage = (event) => {
    const data = JSON.parse(event.data);

    // 프레임 데이터 업데이트
    setCurrentFrame(data);

    // 이미지 표시
    setVideoFrame(data.image_base64);

    // 탐지 결과 업데이트
    setDetections(data.detections);

    // 통계 업데이트
    updateStatistics(data);
  };

  ws.onerror = (error) => {
    console.error('WebSocket error:', error);
    toast.error('Stream connection error');
  };

  ws.onclose = () => {
    console.log('WebSocket closed');
    setIsConnected(false);
  };

  return () => {
    ws.close();
  };
}, [sessionId]);

function updateStatistics(frameData) {
  setFrameCount(prev => prev + 1);
  setTotalDetections(prev => prev + frameData.detections.length);

  // 클래스별 통계 업데이트
  frameData.detections.forEach(det => {
    setClassCounts(prev => ({
      ...prev,
      [det.class]: (prev[det.class] || 0) + 1
    }));
  });
}
```

---

#### 4.5 "Stop Session" 버튼 클릭

**API 호출:**
```javascript
async function handleStopSession() {
  try {
    const response = await fetch(
      `/api/v1/realtime/runs/${sessionId}/stop`,
      {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${token}`
        }
      }
    );

    toast.success('Session stopped');

    // 결과 페이지로 이동
    navigate(`/realtime/sessions/${sessionId}/results`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  }
}
```

---

#### 4.6 세션 결과 페이지

**페이지: `/realtime/sessions/:sessionId/results`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 Session Results: Live Detection Demo                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Session Summary                                              │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Started:  2024-10-04 12:05:00                         │   │
│ │ Ended:    2024-10-04 12:08:34                         │   │
│ │ Duration: 3m 34s                                      │   │
│ │ Status:   ✅ Completed                                │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Performance Statistics                                       │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Total Frames:         6,420                           │   │
│ │ Average FPS:          30.1                            │   │
│ │ Total Detections:     9,753                           │   │
│ │ Avg Det/Frame:        1.52                            │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Class-wise Statistics                                        │
│ ┌─────────┬──────────┬────────────┐                         │
│ │ Class   │ Count    │ Percentage │                         │
│ ├─────────┼──────────┼────────────┤                         │
│ │ person  │ 2,345    │ 24.1%      │                         │
│ │ chair   │ 1,678    │ 17.2%      │                         │
│ │ laptop  │ 1,489    │ 15.3%      │                         │
│ │ bottle  │ 1,234    │ 12.7%      │                         │
│ │ cup     │ 987      │ 10.1%      │                         │
│ └─────────┴──────────┴────────────┘                         │
│                                                               │
│ Saved Frames: 1,284 frames                                  │
│                                                               │
│ ┌────────────────┐  ┌────────────────┐  ┌──────────────┐  │
│ │ Download Frames│  │ Export Report  │  │ New Session  │  │
│ └────────────────┘  └────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**페이지 로드 시 세션 정보 조회:**
```javascript
useEffect(() => {
  fetchSessionResults();
}, [sessionId]);

async function fetchSessionResults() {
  const response = await fetch(`/api/v1/realtime/runs/${sessionId}`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });

  const session = await response.json();
  setSessionData(session);
  setStatistics(session.statistics);
}
```

---

## 시나리오 5: 실험 관리

### 페이지: `/experiments/create`

#### 5.1 실험 생성

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 🧪 Create New Experiment                                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Experiment Name *                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Patch Size Optimization Study                         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Description                                                  │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Investigating optimal patch size for person           │   │
│ │ detection evasion. Testing 4 different sizes.         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Hypothesis                                                   │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ Larger patches achieve better attack success but     │   │
│ │ are more noticeable. Optimal range is 100-150px.     │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Parameters (JSON)                                            │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ {                                                     │   │
│ │   "patch_sizes": [50, 100, 150, 200],                │   │
│ │   "target_class": "person",                           │   │
│ │   "epsilon": 0.6,                                     │   │
│ │   "iterations": 100                                   │   │
│ │ }                                                     │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ Tags (comma-separated)                                       │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ patch-optimization, person-detection, evasion         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌──────────────┐  ┌──────────────┐                         │
│ │   Cancel     │  │ Create 🧪    │                         │
│ └──────────────┘  └──────────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

**"Create" 버튼 클릭:**
```javascript
async function handleCreateExperiment(e) {
  e.preventDefault();

  try {
    const response = await fetch('/api/v1/experiments', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({
        name: formData.experimentName,
        description: formData.description,
        hypothesis: formData.hypothesis,
        parameters: JSON.parse(formData.parametersJson),
        tags: formData.tags.split(',').map(tag => tag.trim())
      })
    });

    const experiment = await response.json();

    toast.success('Experiment created!');
    navigate(`/experiments/${experiment.id}`);

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  }
}
```

---

#### 5.2 실험 실행 및 결과 기록

**페이지: `/experiments/:experimentId`**

**UI 구성:**
```
┌─────────────────────────────────────────────────────────────┐
│ 🧪 Experiment: Patch Size Optimization Study                │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│ Status: ⚡ Active                                            │
│ Created: 2024-10-04 14:00:00                                │
│                                                               │
│ 📝 Description                                               │
│ Investigating optimal patch size for person detection        │
│ evasion. Testing 4 different sizes.                         │
│                                                               │
│ 🎯 Hypothesis                                                │
│ Larger patches achieve better attack success but are more   │
│ noticeable. Optimal range is 100-150px.                     │
│                                                               │
│ 📊 Runs (2/4)                                                │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ✅ Run 1: Patch Size 50                              │   │
│ │    Success Rate: 34% | Detection Drop: 28%           │   │
│ │                                                        │   │
│ │ ✅ Run 2: Patch Size 100                             │   │
│ │    Success Rate: 67% | Detection Drop: 56%           │   │
│ │                                                        │   │
│ │ ⏳ Run 3: Patch Size 150 - In Progress...            │   │
│ │    [████████████░░░░░░] 67%                          │   │
│ │                                                        │   │
│ │ ⏸️  Run 4: Patch Size 200 - Not Started              │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌────────────────┐  ┌────────────────┐                     │
│ │ Add New Run    │  │ View Results   │                     │
│ └────────────────┘  └────────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

---

#### 5.3 실험 Run 추가

**"Add New Run" 버튼 클릭 시 모달:**
```
┌─────────────────────────────────────────────────────┐
│ ➕ Add Experiment Run                               │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Run Name *                                          │
│ ┌─────────────────────────────────────────────┐   │
│ │ Run 3: Patch Size 150                        │   │
│ └─────────────────────────────────────────────┘   │
│                                                      │
│ Parameters (JSON)                                   │
│ ┌─────────────────────────────────────────────┐   │
│ │ {                                            │   │
│ │   "patch_size": 150                          │   │
│ │ }                                            │   │
│ └─────────────────────────────────────────────┘   │
│                                                      │
│ Metrics (JSON)                                      │
│ ┌─────────────────────────────────────────────┐   │
│ │ {                                            │   │
│ │   "attack_success_rate": 0.82,               │   │
│ │   "detection_drop": 0.73,                    │   │
│ │   "training_time": 48.5,                     │   │
│ │   "patch_visibility_score": 0.67             │   │
│ │ }                                            │   │
│ └─────────────────────────────────────────────┘   │
│                                                      │
│ Artifacts (Optional)                                │
│ ┌─────────────────────────────────────────────┐   │
│ │ Patch ID:       [patch-150]                  │   │
│ │ Evaluation ID:  [eval-patch-150]             │   │
│ └─────────────────────────────────────────────┘   │
│                                                      │
│ ┌──────────┐  ┌──────────┐                        │
│ │  Cancel  │  │  Add Run │                        │
│ └──────────┘  └──────────┘                        │
└─────────────────────────────────────────────────────┘
```

**API 호출:**
```javascript
async function handleAddRun(e) {
  e.preventDefault();

  try {
    const response = await fetch(
      `/api/v1/experiments/${experimentId}/results`,
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${token}`
        },
        body: JSON.stringify({
          run_name: formData.runName,
          parameters: JSON.parse(formData.parametersJson),
          metrics: JSON.parse(formData.metricsJson),
          artifacts: {
            patch_id: formData.patchId,
            evaluation_run_id: formData.evaluationId
          }
        })
      }
    );

    const result = await response.json();

    toast.success('Run added successfully!');

    // 실험 페이지 새로고침
    fetchExperimentData();
    closeModal();

  } catch (error) {
    toast.error(`Error: ${error.message}`);
  }
}
```

---

## API 호출 흐름 요약표

### 시나리오 1: 2D 패치 생성

| 순서 | 페이지 | 액션 | API 엔드포인트 | Method | 주요 파라미터 |
|------|--------|------|----------------|--------|---------------|
| 1 | `/attacks/adversarial-patch/create` | 페이지 로드 | `/api/v1/datasets-2d` | GET | skip, limit |
| 2 | 동일 | 페이지 로드 | `/api/v1/models` | GET | skip, limit |
| 3 | 동일 | 데이터셋 선택 시 | `/api/v1/datasets-2d/{id}/top-classes` | GET | limit (기본 5) |
| 4 | 동일 | "Generate" 클릭 | `/api/v1/adversarial-patch/patches/generate` | POST | patch_name, dataset_id, model_id, target_class (파라미터는 백엔드 관리) |
| 5 | `/attacks/adversarial-patch/result/:id` | 페이지 로드 | `/api/v1/adversarial-patch/patches/:id` | GET | - |
| 6 | 동일 | "Download" 클릭 | `/api/v1/adversarial-patch/patches/:id/download` | GET | - |

### 시나리오 2: 노이즈 공격

| 순서 | 페이지 | 액션 | API 엔드포인트 | Method | 주요 파라미터 |
|------|--------|------|----------------|--------|---------------|
| 1 | `/attacks/noise-attack/create` | "Generate" 클릭 (PGD) | `/api/v1/noise-attack/pgd/generate` | POST | attack_name, dataset_id, model_id, epsilon, alpha, iterations |
| 2 | `/attacks/noise-attack/result/:id` | 페이지 로드 | `/api/v1/noise-attack/:id` | GET | - |

### 시나리오 3: 모델 평가

| 순서 | 페이지 | 액션 | API 엔드포인트 | Method | 주요 파라미터 |
|------|--------|------|----------------|--------|---------------|
| 1 | `/evaluation/create` | "Run Evaluation" 클릭 | `/api/v1/evaluation/runs` | POST | name, phase, model_id, dataset_id, config |
| 2 | `/evaluation/runs/:id` | 주기적 폴링 (2초) | `/api/v1/evaluation/runs/:id` | GET | - |
| 3 | `/evaluation/compare` | "Compare" 클릭 | `/api/v1/evaluation/runs/compare` | POST | pre_attack_run_id, post_attack_run_id |

### 시나리오 4: 실시간 카메라

| 순서 | 페이지 | 액션 | API 엔드포인트 | Method | 프로토콜 |
|------|--------|------|----------------|--------|----------|
| 1 | `/realtime/sessions/create` | 페이지 로드 | `/api/v1/realtime/cameras` | GET | HTTP |
| 2 | 동일 | "Start Session" 클릭 | `/api/v1/realtime/runs` | POST | HTTP |
| 3 | `/realtime/sessions/:id/live` | WebSocket 연결 | `ws://*/realtime/stream/:id` | - | WebSocket |
| 4 | 동일 | "Stop Session" 클릭 | `/api/v1/realtime/runs/:id/stop` | POST | HTTP |
| 5 | `/realtime/sessions/:id/results` | 페이지 로드 | `/api/v1/realtime/runs/:id` | GET | HTTP |

### 시나리오 5: 실험 관리

| 순서 | 페이지 | 액션 | API 엔드포인트 | Method | 주요 파라미터 |
|------|--------|------|----------------|--------|---------------|
| 1 | `/experiments/create` | "Create" 클릭 | `/api/v1/experiments` | POST | name, description, hypothesis, parameters |
| 2 | `/experiments/:id` | 페이지 로드 | `/api/v1/experiments/:id` | GET | - |
| 3 | 동일 | "Add New Run" 클릭 | `/api/v1/experiments/:id/results` | POST | run_name, parameters, metrics, artifacts |

---

## 공통 패턴 및 권장사항

### 1. 인증 헤더
모든 API 호출에 JWT 토큰 포함:
```javascript
headers: {
  'Authorization': `Bearer ${localStorage.getItem('access_token')}`
}
```

### 2. 에러 처리
```javascript
try {
  const response = await fetch(url, options);

  if (!response.ok) {
    const error = await response.json();
    throw new Error(error.detail || 'API request failed');
  }

  return await response.json();
} catch (error) {
  toast.error(error.message);
  console.error('API Error:', error);
}
```

### 3. 로딩 상태
```javascript
const [isLoading, setIsLoading] = useState(false);

async function fetchData() {
  setIsLoading(true);
  try {
    // API call
  } finally {
    setIsLoading(false);
  }
}
```

### 4. 폼 검증
```javascript
function validateForm() {
  const errors = {};

  if (!formData.name) {
    errors.name = 'Name is required';
  }

  if (formData.epsilon < 0 || formData.epsilon > 1) {
    errors.epsilon = 'Epsilon must be between 0 and 1';
  }

  return errors;
}
```

### 5. 실시간 업데이트
- **폴링**: 2-5초 간격으로 상태 확인 (평가 진행 중)
- **WebSocket**: 실시간 스트림 데이터 (카메라 세션)

---

## 최근 성능 개선 및 변경사항

### 1. 데이터셋 업로드 개선 (v2.0)
- **새 엔드포인트**: `POST /api/v1/dataset-service/upload-folder`
- **추론 메타데이터 지원**: `inference_metadata_path` 파라미터로 YOLO 추론 결과 업로드
- **트랜잭션 무결성**: 업로드 실패 시 자동 롤백 (DB + 파일 시스템)
- **상세한 에러 메시지**: JSON 라인/컬럼, 파일 권한, 인코딩 오류 등 구체적 정보 제공

### 2. 클래스 통계 조회 최적화
- **새 엔드포인트**: `GET /api/v1/datasets-2d/{dataset_id}/top-classes`
- **성능 개선**: 750배 빠름 (7.5초 → 10ms)
- **메타데이터 캐싱**: 업로드 시 사전 계산된 통계 사용
- **사용 사례**: 타겟 클래스 선택 시 데이터 기반 추천

### 3. 에러 핸들링 강화
모든 API 엔드포인트에서 일관된 에러 응답:

```javascript
// 파일 없음
{
  "detail": "Inference metadata file at /path/to/file.json not found",
  "error_code": "NOT_FOUND"
}

// JSON 파싱 오류
{
  "detail": "Invalid JSON format at line 15, column 23: Expecting ',' delimiter",
  "error_code": "VALIDATION_ERROR"
}

// 권한 오류
{
  "detail": "Permission denied: Cannot read metadata file",
  "error_code": "VALIDATION_ERROR"
}

// 시스템 디렉토리 접근 차단
{
  "detail": "Access to system directories is not allowed",
  "error_code": "VALIDATION_ERROR"
}
```

### 4. 프론트엔드 통합 가이드

**데이터셋 선택 시 자동으로 클래스 통계 로드:**
```javascript
async function onDatasetChange(datasetId) {
  // 1. 데이터셋 선택
  setSelectedDataset(datasetId);

  // 2. 자동으로 상위 클래스 조회 (10ms)
  const topClasses = await fetchTopClasses(datasetId);

  // 3. UI 업데이트
  if (topClasses.source === 'metadata') {
    setRecommendedClasses(topClasses.top_classes);
    setClassOptions(topClasses.top_classes.map(c => c.class_name));
  } else {
    // 메타데이터 없음 - 수동 입력 또는 모델 추론 필요
    setClassOptions([]);
  }
}
```

**업로드 에러 처리 예시:**
```javascript
async function uploadDataset(formData) {
  try {
    const response = await fetch('/api/v1/dataset-service/upload-folder', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
      },
      body: JSON.stringify({
        source_folder: formData.folder,
        dataset_name: formData.name,
        description: formData.description,
        inference_metadata_path: formData.metadataPath  // 선택적
      })
    });

    if (!response.ok) {
      const error = await response.json();

      // 구체적인 에러 메시지 표시
      if (error.error_code === 'NOT_FOUND') {
        toast.error(`파일을 찾을 수 없습니다: ${error.detail}`);
      } else if (error.error_code === 'VALIDATION_ERROR') {
        toast.error(`입력 오류: ${error.detail}`);
      } else {
        toast.error(`업로드 실패: ${error.detail}`);
      }
      return;
    }

    const result = await response.json();

    // 메타데이터 처리 여부 확인
    if (result.metadata_processed) {
      toast.success(
        `데이터셋 업로드 완료! ${result.image_count}개 이미지, ` +
        `메타데이터 분석 완료 (${result.metadata_stats.total_detections}개 탐지)`
      );
    } else {
      toast.success(`데이터셋 업로드 완료! ${result.image_count}개 이미지`);
    }

  } catch (error) {
    toast.error(`네트워크 오류: ${error.message}`);
  }
}
```

---

**작성일**: 2024-10-04
**최종 수정**: 2024-10-04
